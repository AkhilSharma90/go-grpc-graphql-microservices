# destination-rule.yaml
apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: backend-graphql
  namespace: ecommerce-app
spec:
  host: backend-graphql-service
  subsets:
    - name: v1
      labels:
        version: v1
    - name: v2
      labels:
        version: v2