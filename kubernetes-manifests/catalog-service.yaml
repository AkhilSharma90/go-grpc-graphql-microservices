apiVersion: v1
kind: Service
metadata:
  name: backend-catalog-service
  namespace: ecommerce-app
spec:
  selector:
    app: backend-catalog-deployment
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer