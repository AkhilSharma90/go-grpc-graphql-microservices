apiVersion: v1
kind: Service
metadata:
  name: backend-order-service
  namespace: ecommerce-app
spec:
  selector:
    app: backend-order-deployment
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer