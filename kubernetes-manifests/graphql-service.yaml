# service.yaml
apiVersion: v1
kind: Service
metadata:
  name: backend-graphql-service
  namespace: ecommerce-app
spec:
  selector:
    app: backend-graphql
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer