apiVersion: v1
kind: Service
metadata:
  name: backend-elasticsearch-service
  namespace: ecommerce-app
spec:
  selector:
    app: backend-elasticsearch
  ports:
    - protocol: TCP
      port: 9200
      targetPort: 9200
      name: http
    - protocol: TCP
      port: 9300
      targetPort: 9300
      name: transport
  type: ClusterIP
